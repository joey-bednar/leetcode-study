# Notes

any([True False True]) returns True if any in iterable are True
all([i==5 for i in arr]) returns True if all in iterable are True

defaultdict(int) # int, list, set, str
defaultdict(lambda: "empty") # custom default

"string".isalnum()
"string".lower()
